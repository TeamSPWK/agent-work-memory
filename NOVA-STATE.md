# Nova State

- **Goal**: M0 Tech Hypothesis Validation — V0 캡처 무결성 + H1 1분 회상 + H2 매칭/hash chain + H3 10분 RCA 측정. PRD §1.1 line 40 *"더 이상 검증할 기술 가설은 없다"* 단정이 실제로는 *부품 unit test*만 의미하고 *제품 가설*은 미측정임을 본인 dogfooding 1주로 확인. M1 Foundation 진입 전제.
- **Phase**: 시안 → 코드 트랙 closed → M0 Tech Validation — S1 회수 PASS + S1.6 로컬 MVP CONDITIONAL PASS + **S1.6 데이터 baseline 기록 PASS** (사용자 진단 결과 *"NO — 데이터는 있으나 회상·감사 도구 수준 아님"* 7 발견: intent 단편 7/30, 매칭 동일 commit 4세션, 어댑터 25→7 fields, Audit hash fake, Risk fan-out 0, repo 파서 90%, WorkPacket 미요약) + **S1.5 Hash chain CLI PASS** (SHA-256 chain + verify/rebuild/show CLI + 변조 5/5 unit, S1.6 발견 #4 코드 로직 해소) → **M0/S2 자기 캡처 1주 측정 IN PROGRESS** (옵션 C 채택 — fix 보류, 현 수준 baseline) → S1.7 SessionDetail → S3 측정 → S4 PRD 정정 + M1 진입 결정.
- **Blocker**: (1) ~~PRD §5.5 hash chain CLI 미구현~~ **S1.5 PASS로 해소** / (2) ~~SessionDetail seed-id mismatch~~ **S1.7 PASS로 해소** (ingest lookup 우선 + seed fallback + loading 분기) / (3) S1.6 7 발견 — Fix 보류, S2 측정 후 *데이터 기반*으로 우선순위 결정. §6.2 *Audit Layer 자산 재사용 80%*도 영속화 골격만 80%, hash chain CLI는 PASS / (4) AuditTrail.tsx fallback — serve 미실행 시 seed AUDIT_EVENTS의 fake hash가 노출(S1.5 Evaluator Medium #1). UI 디자인 정합성은 S1.6 발견과 함께 *S2 측정 후* 우선순위 결정 / (5) M0/S2 측정 환경 — `awm serve` 꺼져있음, capture hook 본 worktree에 미설치. *사용자 작업 위치에 따라* hook 설치+serve 기동 필요.

## Tasks
| Task | Status | Verdict | Note |
|------|--------|---------|------|
| m2 S1 부트스트랩 | done | PASS — Vite+React19+TS6 strict, CI | web/ + tokens/global.css + fonts |
| m2 S2.1 제품 IA + 골격 | done | PASS(qa) — 평면 네비 6 + /onboarding wizard | navigation seed + Layout 5 |
| m2 S2.2 Today 화면 | done | PASS(qa) — KPI 4 + 타임라인 + 설명 부족 + TODO + 팀 공유 | screens/Today.tsx |
| m2 S2.3 Sessions 목록 | done | PASS(qa) — tool 필터 5 + 검색(intent/actor/repo) + 7행 | screens/Sessions.tsx |
| m2 S2.4 Session Detail | done | PASS(qa) — 대화 7+명령 3+파일 2+매칭 commit 3, s-024 외 mock 한계 배지 | screens/SessionDetail.tsx, seed/sessionDetail.ts |
| m2 S2.5.a ExplainBack | done | PASS(qa) — 5필드 composer + 우측 3카드 | screens/ExplainBack.tsx |
| m2 S2.5.b Share | done | PASS(qa) — pre 요약 + 3채널 + H1 검증 카드 | screens/Share.tsx |
| m2 S2.5.c SelfRecall | done | PASS(qa, 조건부) — split layout, match-line ok/extra, 핸드오프 textarea, 셀프 hardcode 주석 | screens/SelfRecall.tsx |
| m2 S2.6 Audit (H2 4/5) | done | PASS(qa, 조건부) — 4탭 ?tab= 라우팅 + KPI weak 버튼 + mock 한계 배지(role=status) + Billing은 /settings?tab=export 이연 | screens/Audit.tsx + audit/{AuditTrail,Principles,Integrity,PdfPreview}.tsx + seed/audit.ts |
| m2 S2.7.a Risk Radar (H3 1/5) | done | PASS(qa) — 8 risk-tile + DB inline 4행 + 7 카테고리 신호 리스트 + 사고 alert cta → /incidents/INC-26-014 | screens/Risk.tsx + seed/risk.ts |
| m2 S2.7.b Incident hub + Replay + Note (H3 3/5) | done | PASS(qa) — Incident 4탭 wrapper(replay/note 채움, event/reviewer는 S2.7.c placeholder) + incident-canvas 2D timeline + 3 bucket + Note composer + system 감지 mock 배지 | screens/Incident.tsx + incident/{Replay,Note}.tsx + seed/incident.ts |
| m2 S2.7.c Event Detail + Reviewer Brief (H3 5/5) | done | PASS(qa) — EventDetail 핵심 fact 표 + 3분리 + 근거 5건 + 분류 radiogroup 4 + 사유 textarea, ReviewerBrief split 의도/결과 + match-line ok/ok/extra 3 + 승인/차단 actions, Incident 4탭 모두 활성 | incident/{EventDetail,ReviewerBrief}.tsx |
| m2 S2.8 Onboarding 5화면 (H4 5/5) | done | PASS(qa) — Workspace 폼(radiogroup 2 + select 2) + Connect 4 tool + OAuth dialog + Import timeline 4 + Reviewer multi-select + Done KPI 시연 mock + Today 점프 | screens/onboarding/* + seed/onboarding.ts |
| m2 S2.9 Workspace 3탭 (ws 3/3) | done | PASS(qa + Evaluator) — useSearchParams 3탭(members/invite/roles), Members(KPI 4 + 6행 + persona tag/RBAC), Invite(이메일 chip + radiogroup 역할 + 메일 미리보기 동기화), Roles(8 카테고리 × 3 역할 + role=status 변경 안내) | screens/Workspace.tsx + workspace/{Members,Invite,Roles}.tsx + seed/workspace.ts |
| m2 S2.10.a Settings 4탭 (settings 4/5) | done | PASS(qa + Evaluator) — 5탭 wrapper(profile/integrations/notif/export/billing) + Profile(grid 4필드 + 알림 채널 3 + 보안 3 + 위험 액션 region) + Integrations(AI 4 + 외부 3 + 예정 2) + Notifications(5×4 매트릭스 + 무음 시간대 + Slack mock) + AuditExport(3 radiogroup + 5건 export) + Billing 인라인 placeholder | screens/Settings.tsx + settings/{Profile,Integrations,Notifications,AuditExport}.tsx + seed/settings.ts |
| m2 S2.10.b Plan & Billing (**H2 5/5 닫힘**) | done | PASS(qa + Evaluator) — 현재 플랜 카드 + 사용량 progressbar(5/5 100%) + 디자인 파트너 D1 카드 + 5플랜 비교 + 연결제 25% toggle + 세금계산서 4필드 + 청구서 3건 + 토스페이먼츠 결제수단 + 사용량 알림 | screens/settings/Billing.tsx + seed/billing.ts |
| m2.5/S1 Public route 14 스캐폴드 + PublicShell | done | PASS(qa + Evaluator) — PublicShell(banner/contentinfo/region·외부 메뉴 nav) + 14 PublicStub + PUBLIC_BIZ env-aware + 사업자 미등록 placeholder | layout/PublicShell.tsx + routes/public/PublicStub.tsx + seed/public.ts |
| m2.5/S2.a 랜딩 + .pub-* CSS | done | PASS(qa + Evaluator) — 9 섹션 + .pub-* / .hero / .sec / .val-grid / .news-grid / .flow-grid / .law-card / .solo-fold / .faq / .tier-grid CSS | routes/public/Landing.tsx + seed/publicLanding.ts + global.css PUBLIC 섹션 |
| m2.5/S2.b 가격 + 비교표 | done | PASS(qa + Evaluator) — 4 섹션(center h2 + dp-chip role=region + 3 tier full items + aop-def role=note / 비교표 12행 × 3 + .ok/.no 분류 / FAQ 5 + 환불정책 회사페이지 inline link / dark CTA) + .aop-def / .compare / .dp-chip-row / .s-th CSS 추가 + Landing tier-grid role=group 일관성 정렬 | routes/public/Pricing.tsx + seed/publicLanding.ts(PUBLIC_COMPARE 12 + PUBLIC_FAQ_PRICING 5) + global.css |
| m2.5/S2.c 가입/로그인/재설정 3 페이지 | done | PASS(qa + Evaluator, 조건부) — Auth.tsx 3 컴포넌트 + .auth-wrap·.auth-form·.solo-note·.h4-mini·.oauth·.divider·.check·.mail-mock CSS + stub 12→9 + 3 신규 테스트, Evaluator surgical fix 5건 | routes/public/Auth.tsx + global.css 마지막 140줄 + App.test.tsx |
| m2.5/S2.d 회사 + 상태 2 페이지 + 1인 키워드 정리 | done | PASS(qa + Evaluator) — 일반 회사 소개 톤(미션/안하는것/연락+디자인파트너/CTA 4섹션) + Status 6섹션(서비스 5행 + 공개예정 4 + 사고이력 빈 + 알림) + .cmp-twocol·.cmp-block·.cmp-dark·.dontdo·.cmp-biz·.status-list·.status-promise·.status-empty CSS + stub 9→7 + Company/Status 2 신규 테스트 + 룰 §12.3 7항 추가 + 시드/PublicShell/Auth/Landing/Pricing 1인 일괄 정리, Evaluator surgical fix 3건(H1·H2·H3·H4 라벨 TIER+COMPARE 5곳 평이체 + PRD §11.5/§7.5 외부 노출 제거 + Company 섹션당 h2 1개로 강등) | routes/public/{Company,Status}.tsx + global.css 마지막 130줄 + 룰 §3.1 표 |
| m2.5/S2.e 법무 4종 (terms·privacy·refund·business) | done | PASS(qa + Evaluator) — LegalShell + Terms 12 / Privacy 12 / Refund 5 / Business 4 TOC, placeholder 본문 시안 의도 유지, Evaluator surgical fix 4건(TOC sec-{i} id 매핑 + Refund TOC 7→5 + 누락 조 placeholder + CSS #fff 주석) | routes/public/Legal.tsx + seed/publicLegal.ts + global.css |
| m2.5/S2.f 에러 3종 (404·500·maint) + PublicStub 삭제 | done | PASS(qa + Evaluator) — Errors.tsx 3 컴포넌트 + h2→h1 일관성 + 14 라우트 path identity Set | routes/public/Errors.tsx |
| UX Audit S1 — Critical 6/8 surgical fix | done | PASS(qa + Evaluator) — (1) status/accent 토큰 어둡게 색상 대비 5.01~6.53:1 WCAG AA + .tag.green bg 더 진한 blend 5.42 (2) forced-colors fieldset + 글로벌 .btn/.seg/[role=tabpanel]/.link Highlight outline fallback (3) tablist→tabpanel ARIA (Audit/Incident/Settings/Workspace 4화면) + ARIA APG ArrowKey 키보드(tabKeyHandler lib) (4) Sessions 빈 상태 4분기 안내 + .empty-state CSS (5) router lazy 28 라우트 → 청크 분리 Settings 31KB/Incident 30KB/Audit 24KB/Legal 16KB 메인 207KB (6) billing 5-tier→3-tier 외부 PUBLIC_TIERS 정합 + Team ₩100K + Business ₩300K + limit '무제한' 무제한 안내. test 65/65 build PASS | tokens.css + global.css + Audit/Incident/Settings/Workspace.tsx + Sessions.tsx + router.tsx + billing.ts + lib/useTabKeyboard.ts |
| UX Audit S2 — High 11/13 surgical fix | done | PASS(qa + Evaluator) — (1) onboarding Done 가설 H4 카드 통째 제거 (2) Login 재설정 링크 라벨 "비밀번호를 잊으셨나요?" (3) Icon SVG aria-hidden="true" + focusable="false" 명시 (4) Members RBAC→"권한" 평이체 (5) Audit integrity sub hash chain→"변조 방지 서명" 평이체 (6) Landing "(보존 7일)" 인라인 고지 (7) .ic-mark ::before 24×24 클릭 영역 확장 WCAG 2.5.8 (8) Notifications 무음 시간대 카피 + 고위험 즉시 알림 checkbox(quiet-highrisk-exempt default checked + aria-describedby) (9) Signup useState(true)→useState(false) 정통망법 §22 정합 (10) Connect OAuth disabled hint + aria-describedby (11) Sessions useDebounce 200ms + filter useMemo / Risk·Replay useMemo + EVENTS_BY_ID Map / Icon React.memo. Evaluator surgical fix 2건(Sessions 도구 필터 .seg에 role=tab + tabKeyHandler 일관성 + Connect hint 자동화 검증 보강). 테스트 갱신 4건(워크/약관/Landing/Sessions role=tab). lib/{useDebounce,useTabKeyboard}.ts 신규. test 65/65 | screens/{onboarding/Done,workspace/Members,Audit,settings/Notifications,onboarding/Connect,Sessions,Risk,incident/Replay}.tsx + routes/public/{Auth,Landing}.tsx + components/Icon.tsx + styles/global.css |
| UX Audit S3 — Medium 10/12 + Critical Today/Risk fix | done | PASS(qa + Evaluator) — (1) Risk "최근 N건"→실 변수 (2) Workspace onboarding §1→평이체 (3) Invite default Reviewer 무료 (4) Billing ±20% default on (5) Landing CTA Team만 primary 비대칭 (6) prefers-reduced-motion 글로벌 + .timer-chip pulse off (7) solo-note role 의미 분리 유지 (8) mail-mock #0066ff 의도 주석 (9) Privacy 국외이전 Signup 동의 인라인 (10) Err 페이지 /today primary 복귀. Today Hero(.today-hero 카드 + accent border-left + unexplained 분기 CTA) + KPI 4 hero 아래로 + 우측 3카드 유지. Risk 8 tile → 2 group(인프라 4 / 코드 4) 시각 그룹화 + RISK_GROUPS 시드 + group 필드. Evaluator surgical fix 1건(Risk tab 버튼 tabKeyHandler ArrowKey 적용 — 그룹 경계 넘어 8 ids 순환). test 65/65 | screens/{Today,Risk,workspace/Invite,onboarding/Workspace}.tsx + routes/public/{Auth,Errors,Landing}.tsx + seed/{risk,billing}.ts + global.css |
| High Backlog — Icon SVG sprite + 100+ 행 가상화 | backlog | — | 실데이터 연결 후 측정 기반 결정 |
| M0/S1 v1 자산 회수 | done | ✅ PASS | tech-validation/m0 branch + bin/4 + tests/3 + package.json + vitest.config 회수 / npm install 82 pkg 0 vuln / **vitest 23/23** (11/11은 critical만, 전체 23) / `awm help`·`discover` (Claude 2691f/948MB + Codex 26f/783MB) · `ingest --limit 5` 동작 / `docs/verifications/m0-s1-recovery.md` |
| M0/S1.6 v1 UI MVP 로컬 회수 | done | ✅ CONDITIONAL PASS | Architect: v1 UI 회수 *불필요* — main web/이 m2/m2.5 시안 완성본. 진짜 blocker는 빌드 경로 + UI fetch 미연동 / 5 파일 변경 (root scripts.build = `vite build web --outDir ../dist --emptyOutDir` + web/src/lib/useIngest.ts 신규 hook+어댑터 + Today·Sessions·AuditTrail fetch) / 빌드 182ms, tsc strict PASS, 테스트 65+23 / curl 7 endpoints PASS, /api/ingest count=30 + 본인 실 캡처(오늘 nova:auto prompt까지) / Fix 2건 — broken:false + Today recent 의도 주석 / 이슈 backlog 3: [2] useIngest 108줄 (분리), [4] SessionDetail seed-id mismatch (High, scope 외), [5] loading flash (Low) |
| M0/S1.5 Hash chain CLI 최소 구현 | done | ✅ PASS(qa + Evaluator) | `bin/hashchain.mjs` 신규(GENESIS_PREV + canonicalize 재귀 + computeHash + verifyChain + chainAppend + rebuildChain) + `bin/awm.mjs` recordEvent sealEventForChain/readLastChainHash(legacy 차단 throw) + dispatcher `audit verify/rebuild/show` + buildAuditChainView(ingest 응답 노출) + printHelp / `tests/hashchain.test.mjs` 17 case(canonicalize 3 + computeHash 3 + positive 4 + **변조 5 case** + rebuild 2) / `web/src/lib/useIngest.ts` IngestChainEvent + toAuditEventFromChain(chain 기반) + IngestWorkPacket 정리. node 40/40 + web 65/65 + build 168ms + tsc clean. 수동 변조(index 2 summary) → `BROKEN at index 2: hash mismatch` exit 1 + 복원 후 exit 0. rebuild → `.pre-chain-<ISO>` 백업 + idempotent. ingest auditChain ok=true total=5 tail.hash 64자. **S1.6 발견 #4 코드 로직 해소**(useIngest 어댑터가 chain hash 매핑). Medium 1건(AuditTrail.tsx fallback — serve 미실행 시 seed fake hash 노출) backlog. `docs/projects/plans/m0-s1.5-hash-chain.md` + `docs/verifications/m0-s1.5-hash-chain.md` |
| M0/S1.7 SessionDetail seed-id mismatch fix | done | PASS(qa + Evaluator) | `web/src/screens/SessionDetail.tsx` useIngest 호출 + ingest match 우선 + seed fallback + loading 분기(role=status) + isLive 카드(실 캡처 세션 안내 + 기본 메타 dl, role=status) / `web/src/test/setup.ts` 글로벌 vi.mock(useIngest, vi.fn) stub / `web/src/App.test.tsx` S1.7 케이스 2건 추가(실 ingest id → 실 캡처 카드 / loading=true → 로딩 안내). isLive 우선순위 `liveSession && !seedSession`로 seed s-024 풀데이터 보존. web 67/67 + node 40/40 + build 173ms + tsc clean. Evaluator Medium #1(loading 자동화 누락) 조치 완료. 한계: ingest 세션 상세 시안 매핑은 S1.6 baseline #3 어댑터 25→7 확장으로 미루고 S2 측정 후 결정 / 실 브라우저 E2E는 .awm 본 worktree 비어있어 미커버. `docs/verifications/m0-s1.7-session-detail-id-mismatch.md` |
| **M0/S2 자기 캡처 1주 (baseline 기록 후 측정)** | 🔄 IN PROGRESS | — | 본인 실작업 영업일 ≥ 5일 .awm 누적 / V0 손실 0 + 누락률 ≤ 5% / S1.6 baseline (`docs/verifications/m0-s1.6-data-quality-baseline.md` — intent 단편 7/30, repo 파서 90%, 어댑터 25→7 fields) 위에서 1주 후 재측정 |
| M0/S3 H2-b 변조 5/5 명령 측정 | done | PASS | events.jsonl 6행에서 5 시나리오(필드 수정·삽입·삭제·swap·hash 조작) 직렬 실행 → 모두 brokenAt 정확 + 사유 출력 + exit 1, 복원 후 ok. `docs/verifications/m0-h2b-cmd-tampering.md` |
| M0/S3 H1·H2-a·H3 측정 | pending | — | 1분 회상 + 매칭 TP ≥ 80% + 10분 RCA 모의 사고 3건 / S2 본인 dogfooding 1주 데이터 필요 |
| M0/S4 PRD §1.1·§10 정정 + M1 결정 | pending | — | line 40 단정 제거 + M1 Foundation 진입 또는 §11 가설 재검토 결정 |
| M1 Foundation (PRD §10.1) — Stack 결정 포함 | pending | — | M0 PASS 후. Supabase Tokyo · Clerk · Vercel · 토스페이먼츠 4안 중 결정 |

## Recently Done
| Task | Completed | Verdict | Ref |
|------|-----------|---------|-----|
| 디자인 v0.2 외부 14화면 + chat-3.md 10 결정 | 2026-05-11 | PASS — line 359 fix 외 채택 | p0-design-v0.2/chats/chat-3.md |
| 디자인 v0+v0.1 inside-app 23화면 | 2026-05-10~11 | PASS — 자가 점검 9/9 | p0-design-v0/chats/chat-{1,2}.md |

## Known Risks (PRD §11 압축)
| 위험 | 심각도 | 상태 |
|------|--------|------|
| 시장 가설 (한국 SMB Audit 결제 의지) | 높음 | 디자인 lock 완료 → M0 자기 dogfooding → D0 인터뷰 5건 |
| 제품 핵심 가설(H1 1분 회상·H2 매칭/hash chain·H3 10분 RCA) 미측정 | 높음 | M0 Tech Validation sprint S1~S4. FAIL 시 PRD §11 가설 재검토 트리거 |
| v1 자산 회수 ↔ web/ 트랙 의존성 충돌 | 신규 | 별도 branch `tech-validation/m0`에서 진행, main 시각 자산 보호 |
| 1인 운영 키맨 위험 | 높음 | 자동화 + 계약 투명 공개 |
| Audit Layer 외부 신뢰성 | 중간 | v2.x WORM으로 미룸 |
| 인공지능기본법 SMB 강제력 | 중간 | 법무 자문 + FSC 추적 |
| Stack 결정 후 이식 시간 폭주 | 신규 | m2 Plan exit criteria로 통제 |
| a11y WCAG AA 미달 (tabpanel 누락 + 색상 대비 4쌍 + outline:0) | 신규 | /nova:ux-audit Critical — S3 후속 sprint |
| Dark Pattern 외부/내부 플랜 가격 불일치 (EU DSA Art.25 기만 가능) | 신규 | /nova:ux-audit Critical — 단일 시드 통합 또는 명시 |

## 규칙 우회 이력
| 날짜 | 사유 | 사후 조치 |
|------|------|----------|
| — | — | — |

## Last Activity
- Tester Onboarding — `npm run init` 한 줄 + `docs/tester-quickstart.html` 단일 HTML 가이드 + `capture install claude --auto-merge` 자동 머지 → PASS(qa + Evaluator, FAIL 2건 즉시 해소). 사용자 지적 *"8단계 onboarding이 외부 테스터에 비현실적"* → 5분 first-value 압축. **영향 7 파일**: (1) `bin/awm.mjs` — `installClaudeHook({autoMerge})` + `autoMergeClaudeSettings(snippet)` 신규 함수, settings.local.json 깊이 머지(eventName별 그룹 append, command+matcher 기준 dedup) + corrupt JSON throw + CLI `--auto-merge` 옵션 (2) `scripts/awm-init.sh` 신규 — Node ≥ 20 check, npm install(node_modules/.package-lock.json 존재 시 skip), build(dist 있으면 skip), capture install --auto-merge, PID 파일 직접 체크 후 serve 백그라운드, 5단계 색깔 출력 + 마지막 안내 박스 (3) `scripts/awm-serve.sh` (기존 그대로) (4) `package.json` — `npm run init` 추가 (5) `docs/tester-quickstart.html` 신규 self-contained 250줄 — 3 단계(init/Claude 재시작/첫 세션) + FAQ 4건(데이터 위치·hook 영향·종료·제거) + 트러블슈팅 5건(Node·포트·audit 비어있음·브라우저·가이드 stale), inline CSS prefers-color-scheme 다크 자동, Toss-style 외부 페이지 톤(.pub-* 정신), §12.3 외부 노출 금지 7항 통과 (6) `CLAUDE.md` Build & Verify — "외부 테스터 / 신규 환경 — 한 줄" 섹션 + 캡처 hook 섹션 --auto-merge primary로 간소화 (7) `README.md` 최상단 Quick Start 5분 박스 + tester-quickstart.html 링크. **검증**: clean state → npm run init 5/5 PASS + PID 출력 + EOF 박스 / 두 번째 init idempotent(install skip + build skip + hooks "5 already present" + serve "이미 실행 중") / curl /api/health ok / node 40/40 + web 67/67 + tsc clean + build 175ms. **Evaluator FAIL 2건 즉시 해소**: FAIL-1 HTML footer "1인 베타" §12.3 위반 → 제거(외부 노출 금지 7항 7번 운영 인원 규모) / FAIL-2 npm install 멱등 로직 오작동(node_modules -nt 비교 역전) → node_modules/.package-lock.json 존재 체크로 변경. Evaluator 미커버: Node < 20 fail path 실제 실행, Windows, 다크 모드 브라우저 렌더. 산출물: `docs/projects/plans/tester-onboarding.md` + `docs/tester-quickstart.html`. **PRD §11 H4 5분 first-value 가설을 받칠 onboarding 인프라 완비** (8단계 → 1 명령 + 1 재시작). | 2026-05-13
- 개발 편의 — `scripts/awm-serve.sh` (start/stop/restart/status/logs) + npm scripts(`serve`, `serve:stop|restart|status|logs`) 추가 → PASS. 사용자 요청 "로컬 확인용 서버 관리 스크립트". PID/log는 `.awm/serve.{pid,log}` (gitignored). start는 dist 없으면 build → ingest --limit 30 → serve --port 5173 백그라운드. stop은 SIGTERM 5회 폴링 후 SIGKILL fallback. 검증: status idle → start(PID 39501 시작) → curl /api/health ok → restart(PID 51389) → stop → status idle → logs 정상. 환경변수 AWM_PORT / AWM_INGEST_LIMIT override 가능. | 2026-05-13
- M0/S3 H2-b 변조 5/5 명령 수준 측정 (데이터 의존성 0, S2 무관 선행 PASS) → PASS. 메인 push 직후 main checkout 상태에서 자율 진행. Plan H2-b 정의 *"events.jsonl 1행 변조 시 verify 명령 5회 반복 5/5 탐지 + 변조 위치 출력"* 충족. **절차**: (1) capture sample ×6 → events.jsonl 6행 + audit chain ok 확인 (2) `/tmp/h2b-baseline.jsonl` 백업 (3) 시나리오 5건 직렬: 필드 수정(index 0 summary) → brokenAt=0 hash mismatch / 레코드 삽입(index 2 fake) → brokenAt=2 prev mismatch(expected 2a1bf93d, got 00000000) / 레코드 삭제(index 1) → brokenAt=1 prev mismatch / 순서 swap(2-3) → brokenAt=2 prev mismatch / hash 직접 조작(index 4 → ff×64) → brokenAt=4 hash mismatch. 매 시나리오마다 baseline 복원→변조→verify→exit=1 확인 (4) 최종 baseline 복원 → `Audit chain OK — 6 events verified.` exit 0. **5/5 모두 정확** + 사유 type(hash mismatch vs prev mismatch) 적절히 분기. S1.5 unit 17 case와 *두 층위*(unit + CLI 통합)에서 변조 탐지. **의미**: H2-b는 S2 dogfooding 데이터 의존성 0이라 *S3 일부를 선행으로 닫음*. H1·H2-a·H3는 S2 1주 후 측정 대기. `docs/verifications/m0-h2b-cmd-tampering.md` 신규 + NOVA-STATE Tasks 분리(S3 H2-b done, S3 H1·H2-a·H3 pending). .awm 측정 잡음 정리 후 working tree clean. | 2026-05-13
- M0/S1.7 SessionDetail seed-id mismatch fix (NOVA-STATE Blocker #2 해소) → PASS. 위임 받은 자율 진행으로 S1.5 직후 연속 수행. **변경 3 파일**: (1) `web/src/screens/SessionDetail.tsx` — `useIngest()` 추가 + `liveSession = ingestSessions.find(s => s.id === id)` 우선 lookup + `seedSession = SESSIONS.find` fallback + `session = liveSession ?? seedSession` + `isLive = Boolean(liveSession) && !seedSession`로 seed s-024 풀데이터 우선 보존 + loading=true && !session 분기에서 role=status "세션 데이터 불러오는 중…" 표시 + isLive 분기에서 page-h 헤더(intent/tool/actor/repo/when) + role=status 안내 카드("실 캡처 세션 — 시안 상세 데이터 미연결") + 기본 메타 dl(도구/작업자/레포/의도/상태/파일·명령 6 row) 표시, SESSION_DETAIL은 isLive=false 분기에서만 사용 (2) `web/src/test/setup.ts` — useIngest 글로벌 vi.mock stub을 `vi.fn(() => ({...}))` 형식으로 추가하여 case별 mockReturnValueOnce 가능 (3) `web/src/App.test.tsx` — S1.7 케이스 2건 추가: 실 ingest id `claude_abc_flow08_4b8bb4` → 실 캡처 카드 + role=status + 기본 메타 heading / loading=true → 로딩 안내 status. **분기 4종**: s-024 → 풀데이터 / 다른 s-NNN → s-024 데이터 + mock 한계 배지(기존) / claude_XXX → 실 캡처 카드 + dl 기본 메타 / loading && match X → 로딩 안내. **검증**: web 67/67 (기존 65 + S1.7 2) + node 40/40 회귀 + build 173~204ms + tsc clean. **Evaluator(qa-engineer) PASS** Medium #1(loading=true 자동화 누락) 즉시 조치. 미커버: 실 브라우저 E2E(.awm 본 worktree 비어있어 ingest 실데이터 없음, `awm serve` 미기동) + dl grid 위 dt-dd a11y 실 AT 확인. **한계**: 실 ingest 세션의 상세 시안 매핑(대화 맥락·명령·매칭 commit)은 S1.6 baseline #3 어댑터 25→7 확장과 동일 항목으로 *S2 측정 후 데이터 기반* 우선순위 결정. 산출물: `docs/verifications/m0-s1.7-session-detail-id-mismatch.md`. **NOVA-STATE Blocker #2 closed**. 신규 Blocker #5: M0/S2 측정 환경(serve 꺼짐 + capture hook 본 worktree 미설치) — *사용자 작업 위치* 정보가 필요한 운영 결정. | 2026-05-13
- M0/S1.5 Hash chain CLI 최소 구현 (Plan, 구현, Evaluator 독립 서브에이전트) → PASS. PRD §5.5 *"SHA-256 해시 체인 + 무결성 검증 명령"* 충족. **영향 파일 4개**: (1) `bin/hashchain.mjs` 신규 88줄 — pure helper, no fs dep. GENESIS_PREV(`0`×64) + canonicalize(stableReplacer 재귀, hash/prev 필드 제외) + computeHash(SHA-256, `prev\ncanonical` 형식) + verifyChain(첫 깨진 인덱스 + 사유 반환) + chainAppend + rebuildChain (2) `bin/awm.mjs` ±95줄 — recordEvent에 sealEventForChain/readLastChainHash 통합(legacy hash 없는 tail 발견 시 *명시 throw*로 silent chain 시작 차단) + main dispatcher에 `audit verify/rebuild/show --last N` 3 명령 + buildAuditChainView(events.jsonl tail N → ingest.json auditChain.{ok, total, head, brokenAt, tail[]} 노출) + printHelp 갱신 (3) `tests/hashchain.test.mjs` 신규 156줄 — 17 case (canonicalize order-independence 3 + computeHash determinism/length 3 + positive empty/1/3/100 4 + **변조 5 case**: 필드 수정 / 레코드 삽입 / 삭제 / 순서 swap / hash 필드 직접 조작 + rebuild idempotency 2) (4) `web/src/lib/useIngest.ts` ±40줄 — IngestChainEvent + IngestAuditChain 타입 + toAuditEventFromChain(chain 기반 매핑, hash/prev 12자 truncate 디스플레이용) + 미사용 IngestWorkPacket 정리. **검증**: node 40/40 (기존 23 + hashchain 17) + web 65/65 + vite build 168ms + `cd web && npx tsc --noEmit` clean. **실 흐름**: `awm capture sample` ×3 → `awm audit show --last 3` prev 체인 연결 + GENESIS_PREV에서 시작 확인 → `awm audit verify` `Audit chain OK — 3 events verified` exit 0 → 한 줄 summary 변조 → `BROKEN at index 2: hash mismatch — recomputed e0f39549…, stored ac180a43…` exit 1 + id/createdAt 정확 표시 → 원본 복원 → exit 0 → `awm audit rebuild` `.awm/events.jsonl.pre-chain-2026-05-13T00-32-55-274Z` 백업 생성 + 재해시 + idempotent(2회 호출 동일 hash) → `awm ingest --limit 30` ingest.json auditChain.ok=true total=5 head 64자 tail[0].hash 64자. **S1.6 발견 #4 코드 로직 해소** — useIngest 어댑터가 더 이상 `id.slice(-8)`·`'--------'`·`'session.end'` 더미를 만들지 않고 events.jsonl chain의 실 hash/prev/event를 매핑. **Evaluator 미커버**: `awm serve` 기동 후 브라우저 실 렌더링(E2E), 대용량 events 성능, 동시 캡처 race condition — S1.5 scope 외. **Medium #1 backlog**: AuditTrail.tsx L13 `ingest.auditEvents.length > 0 ? : AUDIT_EVENTS` fallback이 serve 미실행 시 seed fake hash 노출 — UI 디자인 정합성은 S1.6 발견들과 함께 S2 측정 후 우선순위 결정(Blocker #4 신규). **Low #1 조치 완료**: 미사용 IngestWorkPacket 타입 + IngestData.workPackets 필드 제거. 산출물: `docs/projects/plans/m0-s1.5-hash-chain.md` + `docs/verifications/m0-s1.5-hash-chain.md`. **H2-b 측정 진입 가능** (S3). | 2026-05-13
- M0/S1.6 데이터 품질 baseline 기록 + M0/S2 측정 진입 결정 (옵션 C — 정직한 measurement-driven) → PASS. 사용자가 `localhost:5173` 화면을 본인 30 세션 데이터로 직접 보면서 *"정말 사용할만한 수준의 데이터로 보이는지 확인해줘"* 요청. 결정: **NO — 데이터는 있으나 회상·감사 도구 수준은 아님.** 7 발견(사용자 화면에서 직접 인지 가능): (1) Sessions intent가 *대화 단편* — 30 중 7건이 20자 미만 ("오케이 릴리스 진행", "진행하세요", "다음 작업은?" 식 답글), H1 1분 회상 직접 위협 (2) 매칭이 *시간 근접*에 의존 — flow04·05·06·07 4 세션이 *동일한 3 commit* 추천, P1 4축 가중이 시간 dominant, H2-a TP 위협 (3) 어댑터(useIngest.ts toSessionSeed)가 데이터 깊이 평탄화 — ingest.json sessions 25 fields 중 *7만 사용* (28%), agentSummary/workBrief/explainBack/flowSteps/evidence/unresolved/confirmedCommits/linkedCommits 통째로 unused (4) Audit hash chain은 **fake** — `hash: id.slice(-8)`, `prev: '--------'`, `broken: false`, `type: 'session.end'` 모두 더미, PRD §5.5 미구현(S1.5)을 UI에서 가시화 (5) Risk 세션 fan-out 0 — sessions 30 모두 `risk: null`, workPackets riskCount>0 4/24의 정보가 세션·Risk Radar로 전파 X (6) repo 파서 정합 *90%* — `05/12`, `YYYY-MM-DD/new-chat` 형식 같은 날짜·세션 경로 파편 3건 (`bin/awm.mjs` cwd 추출 일부 형식 실패, 구체 사례는 baseline 문서) (7) WorkPacket summary 100% *원문 잘림* — agentSummary 있음에도 intentSummary 그대로 사용. 의미: PRD §1.1 line 40 *"더 이상 검증할 기술 가설은 없다"* 단정이 *부품 합격*에만 한정된 명제임을 실 데이터로 직접 증명. M0/S4 PRD 정정 항목에 본 baseline 인용. 옵션 3안 제시 — A(어댑터 surgical 강화)·B(데이터 파이프라인 fix)·C(현 수준 baseline + S2 측정 시작) 중 **사용자가 C 채택**. measurement 없이 surgical fix는 *측정 정직성*을 흐림. M0/S2 1주 후 동일 7 차원 재측정으로 *어디부터 fix할지* 데이터 기반 결정. 산출물: `docs/verifications/m0-s1.6-data-quality-baseline.md` 신규(수치 표 + 7 발견 + S2 비교 기준 표) / NOVA-STATE Goal·Phase·Blocker·Tasks 갱신. awm serve PID 90774 LISTEN 5173 유지 — 사용자 자기 작업 계속 캡처. | 2026-05-13
- M0/S1.6 v1 UI MVP 로컬 회수 (Architect → Generator → Evaluator → Fix, orch-mp2ow67o-he05) → ✅ CONDITIONAL PASS. 사용자 미션 "로컬에서 사용할 수준의 MVP 만들고 오세요 (Today/Sessions/Audit 메뉴를 실제로 사용해볼 수 있도록)" + nova:auto 자율 권한. **핵심 발견 (Architect)**: v1 UI 회수 *불필요* — 현재 main web/이 m2/m2.5 시안 완성본(Today + Sessions + Audit 3 screens + router + dist). v1 회수는 퇴행이고 진짜 blocker는 (a) 빌드 경로 — `awm serve`는 `<root>/dist/` 기대인데 vite는 `web/dist/`로 산출 (b) UI ↔ /api/ingest 미연동 — 정적 seed import만 있어 본인 캡처 안 보임. 사용자에게 1회 확인 후 "web/ + fetch 연동 최소" 옵션 채택. **Generator (senior-dev) 5 파일 변경**: (1) root `package.json scripts.build` = `"vite build web --outDir ../dist --emptyOutDir"` — vite8은 --root 플래그 미지원, positional root + outDir로 해결 (2) `web/src/lib/useIngest.ts` 신규 hook+어댑터 108줄 — IngestSession→SessionSeed, IngestWorkPacket→AuditEvent 매핑 + fetch+useEffect+cancelled flag + fallback (3) Today.tsx fetch 주입 + base = isLive ? ingest.sessions : SESSIONS (4) Sessions.tsx 동일 (5) screens/audit/AuditTrail.tsx 동일. **Evaluator (qa-engineer) Standard 검증**: 빌드 185ms exit 0 + dist/index.html / `cd web && npm test` 65/65 + `npx vitest run` 23/23 / `tsc --noEmit` strict PASS / curl 7 endpoints — `/` 200, `/api/health` ok:true, `/api/ingest` count=30 + workPackets:24 실 데이터, sessions[0] schema 25 keys, SPA shell 정합 / 이슈 5건: [1] High Today recent 필터 OR 조건이 데이터 자체를 통과시켜 30건 전부 "오늘" 표시 → Fix [2] Medium useIngest 108줄(가이드 50 초과, 분리 가능) → backlog [3] Medium toAuditEvent broken 필드 누락 silent 소실 → Fix [4] High SessionDetail seed-id mismatch (실 id `claude_XXX_flowYY_zzzz` vs seed `s-NNN`) → S1.7 분리 [5] Low loading flash → 허용. **Fix 2건**: (a) useIngest.ts toAuditEvent return에 `broken: false` 1줄 (Audit 체인 깨짐 표시 silent 소실 차단) (b) Today.tsx L18-21에 *왜 OR 조건이 의도적인지* inline comment — ingest --limit 30 응답의 startedAt에 날짜 정보가 없어 시안 카피 "오늘"이 정확히 *오늘만* 의미하지 않음 명시, 카피 정정은 M0/S2 측정 후 (시안 lock 정합). **Fix 후 회귀**: 빌드 182ms PASS, ingest --limit 30 (Sessions 30 + Risks 5 + Repos 6) + serve 백그라운드 + curl / 200 + /api/health ok + /api/ingest count=30 + 본인 실 캡처 세션 (ex1=`claude_38e03_flow08_4b8bb4` intent에 *오늘 사용자가 입력한 nova:auto prompt 자체*가 들어있음 — V0 캡처 회로 작동 1차 증거) + serve kill. **사용자 사용 가이드**: `git checkout tech-validation/m0` → `npm run build` → `node bin/awm.mjs ingest --limit 30` → `node bin/awm.mjs serve --port 5173` → 브라우저 `http://127.0.0.1:5173/today`. orchestration completed. | 2026-05-13
- M0 Tech Hypothesis Validation 진입 결정 + 측정 프로토콜 작성 → PASS. `tech-validation/m0` branch에서 v1 자산 9파일 회수, vitest 23/23, awm CLI 동작 (Claude 2691f/948MB + Codex 26f/783MB 탐지). 발견 2: (1) NOVA-STATE "11/11 critical"은 23 중 critical만 — 부품 카운트가 제품 가설로 잘못 해석 (2) PRD §5.5 hash chain CLI v1 미구현 → S1.5. 산출물: `docs/projects/plans/m0-tech-hypothesis-validation.md` (CPS + 4가설 측정 표 + S1~S4) + `docs/verifications/m0-s1-recovery.md`. | 2026-05-12
- 시안 → 코드 트랙(m2 inside-app 28+1 + m2.5 외부 14 + UX Audit 3 sprint Critical 8/8 + High 11/13 + Medium 10/12) closed. 상세 — git log + `docs/projects/STATUS.md` 참조. | ~2026-05-12

## Refs
- m2 S2.6 Plan: `docs/projects/plans/m2-s2.6-audit.md`
- m2 S2.7 Plan: `docs/projects/plans/m2-s2.7-h3.md` (3 sub-sprint 분할)
- m2 S2.8 Plan: `docs/projects/plans/m2-s2.8-onboarding.md`
- **프로젝트 현황판: `docs/projects/STATUS.md`** (페이즈·sprint·화면 매트릭스·보류 결정·프로토타입 흔적)
- 디자인 lock: p0-design-v0/(inside-app 23화면) + p0-design-v0.2/(외부 14화면)
- Prompts: p0-...md(1차) / p0.1-...round2.md(2차) / p0.2-...round3.md(3차)
- Plans: m2-frontend-from-design.md(inside-app) / m2.5-public-pages-from-design.md(외부)
- PRD v2.1 / 협업 룰 .claude/rules/* / v1 archive: `git checkout legacy-v1`
