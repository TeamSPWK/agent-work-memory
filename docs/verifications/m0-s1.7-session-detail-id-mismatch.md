# M0/S1.7 — SessionDetail seed-id mismatch fix 검증

> Plan: NOVA-STATE Blocker #2 직결. branch: `tech-validation/m0`. 2026-05-13.

## 결론

**PASS** (Standard 검증, qa-engineer 독립 서브에이전트).

## 문제

`web/src/screens/SessionDetail.tsx`가 seed `SESSIONS` (id 형식 `s-NNN`)에서만 lookup → 실 ingest 세션 id(`claude_XXX_flowYY_zzzz`)로 진입 시 *"세션을 찾을 수 없습니다"* 404. S1.6 baseline 발견.

## 변경

| 파일 | 변경 |
|------|------|
| `web/src/screens/SessionDetail.tsx` | useIngest 호출 + ingest lookup 우선 + seed fallback + loading 상태 + isLive 분기(실 캡처 카드 + 기본 메타 dl, role=status 안내) |
| `web/src/test/setup.ts` | `vi.mock('../lib/useIngest', () => ({ useIngest: vi.fn(...) }))` 글로벌 stub (mockReturnValueOnce 가능하게 `vi.fn` wrap) |
| `web/src/App.test.tsx` | S1.7 케이스 2건 — 실 ingest id → 실 캡처 카드 / loading=true → 로딩 안내 |

## 분기 동작

| id | match | 표시 |
|----|-------|------|
| `s-024` | seed match | 기존 SESSION_DETAIL 풀데이터 (대화/명령/파일/매칭 commit) |
| 다른 `s-NNN` | seed match | s-024 데이터 + mock 한계 배지 (기존 동작) |
| `claude_XXX_flowYY_zzzz` | ingest match | "실 캡처 세션 — 시안 상세 미연결" 카드 + 기본 메타 dl |
| loading 중 + match X | — | "세션 데이터 불러오는 중…" role=status |
| 그 외 | 매치 X | 기존 404 |

isLive 우선순위: `liveSession && !seedSession` → seed s-024가 *우선*(시안 풀데이터 보존).

## 검증

- `cd web && npm test -- --run` — **67/67 PASS** (기존 65 + S1.7 2건)
- `cd web && npx tsc --noEmit` — clean
- `npm run build` (root) — 173~204ms, dist 산출
- `npx vitest run` (root) — 40/40 회귀 (S1.5 hashchain 17 + 기존 23)

## Evaluator(qa-engineer) 발견 처리

- **Medium #1 loading=true 자동화 없음** → 조치 완료 (App.test.tsx 케이스 1건 추가, 67/67)
- **Low (a11y 실측)**: dl grid 위 dt-dd 쌍은 WCAG 기술적 유효, AT 실측은 환경 제약 — 별도 수동 검증 항목

## Blocker 처리

- **Blocker #2 SessionDetail seed-id mismatch**: 해소. Sessions/Today에서 실 ingest 세션 클릭 시 더 이상 404 안 남.

## 한계 (S2 측정 후 결정)

- ingest 세션의 *상세 시안 데이터*는 미연결 — S1.6 baseline 발견 #3(어댑터 25→7 fields)와 동일 항목. *S2 측정 후 데이터 기반*으로 어떤 필드를 풀데이터에 매핑할지 결정.
- 실 브라우저 E2E — `awm serve` 기동 + Sessions 목록에서 실 세션 클릭 → SessionDetail 렌더 확인은 수동 검증 항목 (현재 본 worktree .awm 비어있음).
